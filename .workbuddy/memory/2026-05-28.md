# 2026-05-28 工作日志

## Git 分支同步与 IDE 配置提交

- 本地 `master` 分支追踪的 `origin/master` 已不存在，远程已改为 `main` + `develop` 分支结构
- 以远程 `origin/main` 为准，重置本地 `main` 分支（`git reset --hard origin/main`）
- 创建并提交两个 IDE 配置文件：
  - `.claude/settings.json` — Claude IDE 权限配置
  - `.hbuilderx/launch.json` — HBuilderX uniCloud 启动配置（aliyun provider）
- 提交信息：`chore: 添加 IDE 配置文件`（commit `e06d229`）
- 本地领先远程 1 个提交，待推送（HTTPS 凭证管理器在沙箱中无法交互，需用户手动执行 `git push origin main`）

## 注意事项

- 全局 `.gitconfig` 中 `credential.helper = manager-core` 已弃用，实际可用程序为 `git-credential-manager.exe`
- SSH 连接 GitHub 被网络拦截（IP 198.18.x.x），暂无法通过 SSH 推送
